$('#body_part_genres').html("<%= j(render "admin/body_part_genres/index", body_part_genres: @body_part_genres) %>");
$('#flash-message').html('<%= j(render "layouts/flash_message") %>');
$('#erorr').html("<%= j(render 'layouts/error_messages', model: @body_part_genre) %>");

function sleep(msec) {
   return new Promise(function(resolve) {

      setTimeout(function() {resolve()}, msec);

   })
};

$(document).ready(function (){
  $('#flash-message').slideDown(1000,async function start(){
    await sleep(2000);
    $('#flash-message').slideUp(1000);
  });
});