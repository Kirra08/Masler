$('#gear_genres').html("<%= j(render "admin/gear_genres/index", gear_genres: @gear_genres) %>");
$('#flash-message').html('<%= j(render "layouts/flash_message") %>');
$('#erorr').html("<%= j(render 'layouts/error_messages', model: @gear_genre) %>");
$(".text_field").val("");
$(".file_field").val("");

function sleep(msec) {
   return new Promise(function(resolve) {

      setTimeout(function() {resolve()}, msec);

   })
};

$(document).ready(function (){
  $('#flash-message').slideDown(1000,async function start(){
    await sleep(2000);
    $('#flash-message').slideUp(1000);
  });
});