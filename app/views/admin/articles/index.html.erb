<div class="container">
  <div class="row">
    <div class="col-lg-8">
      <table class="table">
        <thead>
          <tr>
            <th>投稿ユーザー名</th>
            <th>投稿タイトル</th>
            <th>投稿日時</th>
            <tr></tr>
          </tr>
        </thead>
        <tbody>
          <% @articles.each do |article| %>
            <tr>
              <td><%= link_to article.user.name, admin_user_path(article.user) %></td>
              <td><%= link_to article.title, admin_article_path(article) %></td>
              <td><%= article.created_at.strftime('%Y/%m/%d') %></td>
              <td><%= link_to "投稿を削除する", admin_article_path(article), method: :delete, data: {confirm: "本当に削除しますか？"} %></td>
            </tr>
          <% end %>
        </tbody>
      </tabla>
    </div>
  </div>
</div>